# exam_app — full School API (version A)

This is a full example Django project implementing School, Classroom, Teacher, Student APIs
with Django REST framework and django-filter.

Run:
    pip install -r requirements.txt
    python manage.py migrate
    python manage.py runserver
